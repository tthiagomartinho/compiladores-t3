algoritmo teste1;

variaveis
	martinho, martinho1, martinho3 : matriz [10][10] de inteiro;
	media : inteiro;
	martinho4, martinho5, martinho6 : matriz [10][10] de literal;
	raizQuadrada : real;
	teste: logico;
fim-variaveis

funcao soma(x : inteiro, y : logico) : 
	raizQuadrada : real;
	inicio
		retorne;
	fim
fim-funcao

funcao soma2(x : inteiro, y : real, k : inteiro, w : literal) :
	raizQuadrada : real;
	inicio
		retorne;
	fim
fim-funcao

funcao soma1(x : inteiro, y : logico) : inteiro
	raizQuadrada : real;
	teste: logico;
	rt : inteiro;
	inicio
		retorne 1;
	fim
fim-funcao

inicio
	media = (((2 + media) * 2) * media) + 4 + 151 + (((2 + media) * 2) * media) / 8 + 7^2 + 3%4;
	//media = 2.2 + 3;
//	martinho[1][9] = soma1(1, media);
	media++;
	//raizQuadrada = 2 + 3;
	//teste[10] = (4 < martinho[10]) e (4 > martinho[1]);
	teste = (verdadeiro == teste) e (falso <> teste) e (((teste <> falso) e verdadeiro) ou teste);
	se nao teste == verdadeiro entao
		media = media + 3;
	senao
		media = media + 4;
	fim-se
	soma(2, teste);

fim
